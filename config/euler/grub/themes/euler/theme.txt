# Euler GRUB theme — Scandinavian light, minimal
# Compatible con gfxterm 1440p/4K
# Palette: canvas #FFFFFF, ink 87% #000000, secondary 60% #00000099, selected 87% #000000DE, fill 6% #0000000F
# corner-radius: 12 — gfxterm does not natively clip radius; keep as design token (supported by some forks via + boot_menu corner-radius)

+ boot_menu {
  left = 30%
  top = 30%
  width = 40%
  height = 40%
  # alternative 42% min(720) centered — gfxterm uses % only; 40% ~ 576px@1440p / 42% ~ 605px, both centered
  item_height = 40
  item_spacing = 8
  align = center
  # padding: 12 — outer padding for pill list (apply via item spacing + background fill)
  selected_item_color = "#000000DE"
  selected_item_font = "Euler Regular 16"
  selected_item_background = "#0000000F"
  item_color = "#00000099"
  item_font = "Euler Regular 16"
}

+ label { text = "Euler OS" color = "#000000" font = "Euler Regular 24" align = center }

desktop-image: "background.png" # asset neutralized to solid white with 4% grid
desktop-color: "#FFFFFF"
title-color: "#000000"
title-font: "Euler Regular 18"
